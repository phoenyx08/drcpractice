<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Football Tribunal Practice{% endblock %}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }
        
        /* Header */
        .header {
            background-color: #0066cc;
            color: white;
            padding: 1rem 0;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }
        
        .header-content {
            display: flex;
            align-items: center;
            justify-content: space-between;
        }
        
        .logo {
            font-size: 1.5rem;
            font-weight: bold;
        }
        
        .site-title {
            font-size: 1.8rem;
            font-weight: bold;
        }
        
        /* Navigation */
        .nav {
            background-color: #004499;
            padding: 0;
        }
        
        .nav-menu {
            list-style: none;
            display: flex;
            flex-wrap: wrap;
        }
        
        .nav-item {
            position: relative;
        }
        
        .nav-link {
            display: block;
            padding: 1rem 1.5rem;
            color: white;
            text-decoration: none;
            transition: background-color 0.3s;
        }
        
        .nav-link:hover {
            background-color: #0066cc;
        }
        
        .nav-item.active .nav-link {
            background-color: #0066cc;
        }
        
        .dropdown {
            position: absolute;
            top: 100%;
            left: 0;
            background-color: #003366;
            min-width: 200px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.2);
            display: none;
            z-index: 1000;
        }
        
        .nav-item:hover .dropdown {
            display: block;
        }
        
        .dropdown-item {
            padding: 0.8rem 1.5rem;
            color: white;
            text-decoration: none;
            display: block;
            border-bottom: 1px solid #004499;
        }
        
        .dropdown-item:hover {
            background-color: #004499;
        }
        
        /* Main Content */
        .main {
            min-height: calc(100vh - 200px);
            padding: 2rem 0;
        }
        
        .breadcrumbs {
            background-color: #f8f9fa;
            padding: 0.5rem 0;
            margin-bottom: 2rem;
            border-radius: 5px;
        }
        
        .breadcrumbs-list {
            list-style: none;
            display: flex;
            align-items: center;
        }
        
        .breadcrumbs-item {
            color: #666;
        }
        
        .breadcrumbs-item:not(:last-child)::after {
            content: ' > ';
            margin: 0 0.5rem;
            color: #999;
        }
        
        .breadcrumbs-link {
            color: #0066cc;
            text-decoration: none;
        }
        
        .breadcrumbs-link:hover {
            text-decoration: underline;
        }
        
        .content-area {
            background-color: white;
            padding: 2rem;
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }
        
        .content-title {
            color: #0066cc;
            margin-bottom: 1.5rem;
            font-size: 2rem;
        }
        
        .categories-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 1.5rem;
            margin-top: 2rem;
        }
        
        .category-card {
            background-color: #f8f9fa;
            padding: 1.5rem;
            border-radius: 5px;
            border-left: 4px solid #0066cc;
        }
        
        .category-title {
            color: #0066cc;
            margin-bottom: 0.5rem;
            font-size: 1.2rem;
        }
        
        .category-link {
            color: #0066cc;
            text-decoration: none;
            font-weight: bold;
        }
        
        .category-link:hover {
            text-decoration: underline;
        }
        
        .decisions-list {
            list-style: none;
            margin-top: 1rem;
        }
        
        .decision-item {
            padding: 0.5rem 0;
            border-bottom: 1px solid #eee;
        }
        
        .decision-link {
            color: #0066cc;
            text-decoration: none;
        }
        
        .decision-link:hover {
            text-decoration: underline;
        }
        
        .decision-meta {
            color: #666;
            font-size: 0.9rem;
            margin-left: 1rem;
        }
        
        /* Footer */
        .footer {
            background-color: #333;
            color: white;
            text-align: center;
            padding: 2rem 0;
            margin-top: 3rem;
        }
        
        .footer-content {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }
        
        /* Responsive */
        @media (max-width: 768px) {
            .header-content {
                flex-direction: column;
                text-align: center;
            }
            
            .nav-menu {
                flex-direction: column;
            }
            
            .dropdown {
                position: static;
                display: block;
                box-shadow: none;
                margin-left: 1rem;
            }
            
            .categories-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
    {% block stylesheets %}{% endblock %}
</head>
<body>
    <header class="header">
        <div class="container">
            <div class="header-content">
                <div class="logo">FT</div>
                <h1 class="site-title">Football Tribunal Practice</h1>
            </div>
        </div>
    </header>
    
    <nav class="nav">
        <div class="container">
            <ul class="nav-menu">
                {% block navigation %}{% endblock %}
            </ul>
        </div>
    </nav>
    
    <main class="main">
        <div class="container">
            {% block breadcrumbs %}{% endblock %}
            
            <div class="content-area">
                {% block content %}{% endblock %}
            </div>
        </div>
    </main>
    
    <footer class="footer">
        <div class="footer-content">
            <p>&copy; {{ 'now'|date('Y') }} Football Tribunal Practice. All rights reserved.</p>
        </div>
    </footer>
    
    {% block javascripts %}{% endblock %}
</body>
</html>
